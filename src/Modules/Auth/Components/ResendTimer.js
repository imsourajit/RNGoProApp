import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ResendTimer = props => {
  return (
    <View>
      <Text>Resend Timer</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  main: {
    backgroundColor: '#000000',
  },
});

export default ResendTimer;
